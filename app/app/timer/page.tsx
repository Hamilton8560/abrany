import FocusSession from "@/components/timer/FocusSession";

// The full timer view is a self-contained hero (its own heading + big ring).
// Timer state is provided globally by <TimerProvider> in the app layout.
export default function TimerPage() {
  return <FocusSession />;
}
